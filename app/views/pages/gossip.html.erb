
<div style="display: flex; flex-direction: row; justify-content: center; align-items: center; line-height: 1em; font-size: 5em; margin-bottom:20px">
  <h1 style="font-size: 1em">Vous êtes sur le potin <br>de <%= Gossip.find(params[:id].to_i).user.first_name %> <%= Gossip.find(params[:id].to_i).user.last_name %></h1>
</div>

<main class="container">
<div style="margin-bottom: 20px;"><%= link_to "Voir le profil de l'auteur", user_path(Gossip.find(params[:id].to_i).user.id), :class => "btn btn-info" %></div>
<div class="card">
  <div class="card-header">
    <%= Gossip.find(params[:id].to_i).title %> - id potin = <%= Gossip.find(params[:id].to_i).id %>
  </div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <p><%= Gossip.find(params[:id].to_i).content %></p>
      <footer class="blockquote-footer"><%= link_to "#{Gossip.find(params[:id].to_i).user.first_name} #{Gossip.find(params[:id].to_i).user.last_name}", user_path(Gossip.find(params[:id].to_i).user.id) %> le <%=Gossip.find(params[:id].to_i).created_at%></footer>
    </blockquote>
  </div>
</div>

</main>
